NODE_ENV=
PORT=
API_PREFIX=
SWAGGER_ENABLED=

# RabbitMQ
RABBITMQ_URL=
EVENTS_EXCHANGE=
# This worker's own mailbox on the broker. Caps so it is never mistaken
# for the routing key below — a queue is *who consumes*, a routing key is
# *what happened*. The dead-letter queue is derived as <name>_DEAD.
DOCUMENT_WORKER_QUEUE=

# The event this worker binds to. A contract with the publisher, not a
# preference: if this and the publisher disagree, the exchange routes the
# message to no queue and drops it silently. Change both together.
LEASE_CREATED_ROUTING_KEY=

RABBITMQ_PREFETCH=

# Object storage
STORAGE_ENDPOINT=
STORAGE_BUCKET=
STORAGE_ACCESS_KEY_ID=
STORAGE_SECRET_ACCESS_KEY=
STORAGE_REGION=
